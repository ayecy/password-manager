# password-manager
First try of the Java Password Manager working with encrypted data

⚙️ Функциональные возможности
1) Создание и использование мастер-пароля (ключа шифрования).
2) Добавление, просмотр и удаление записей вида:
       сайт → логин → пароль
3) Шифрование и дешифрование всех паролей при сохранении/чтении.
4) Хранение данных в локальном файле passwords.txt.
5) Базовый интерфейс (JavaFX):
    - Поля для ввода сайта, логина и пароля;
    - Кнопки "Добавить", "Показать", "Удалить", "Сохранить";
    - Мини-таблица для просмотра записей.
  
🧠 Технологический стек
Язык программирования: Java 17+
UI-библиотека: JavaFX
Алгоритм шифрования: AES-GCM-128 (Advanced Encryption Standard)
Формат хранения: Текстовый файл (ключ=значение, Base64)
IDE: VS Code (с поддержкой JavaFX)


🧩 Архитектура проекта
src/
 ├── model/
 │    ├── PasswordEntry.java        # Класс одной записи (сайт, логин, пароль)
 │    └── EncryptionUtil.java       # Методы encrypt/decrypt с AES 
 │
 ├── service/
 │    └── PasswordManager.java      # Управление коллекцией и файлами
 │
 ├── ui/
 │    └── MainApp.java              # Основной класс с JavaFX интерфейсом
 │
 └── Main.java                      # Точка входа (запуск приложения)


🔐 Принцип шифрования
1) Каждый пароль шифруется с помощью AES/GCM/NoPadding 16 byte key (учебная реализация).
2) Ключ шифрования формируется из мастер-пароля, введённого пользователем при запуске.
3) Зашифрованные данные кодируются через Base64 для безопасного хранения в текстовом виде.

